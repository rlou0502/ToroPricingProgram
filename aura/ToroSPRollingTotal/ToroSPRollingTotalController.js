({
    updateRollingTotals : function(cmp, event) {
        console.log('@ToroSPRollingTotalController:updateRollingTotals')
        var params = event.getParam('arguments');
        console.log('params:');
        console.log(params);
        if (params) {
            // params.spQuantity
            // params.spContribution
            // params.spDNetPrice
		}
    }
})